import 'package:flutter/material.dart';

class DashboardFavourite extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Favourite page"),
      ),
    );
  }
}
